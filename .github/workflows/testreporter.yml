name: Remote Dispatch Action Initiator

on:
  push:
  repository_dispatch:

jobs:
  ping-pong:
    runs-on: ubuntu-latest
    steps:
      - name: PING - Dispatch initiating repository event
        if: github.event.action != 'pong'
        run: |
          curl -XPOST -u "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" -H "Accept: application/vnd.github.everest-preview+json" -H "Content-Type: application/json" https://api.github.com/repos/RAMAKREDDY/wdioreportportal/actions/workflows/main.yml/dispatches --data '{"ref": "main"}'
          
